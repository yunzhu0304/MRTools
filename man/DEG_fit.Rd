% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DEG_fit.R
\name{DEG_fit}
\alias{DEG_fit}
\title{Format DEG List for MRTools Analysis}
\usage{
DEG_fit(
  data,
  GeneSymbol,
  logFC_from,
  Pvalue_from,
  logFC_THR = 1,
  P.Value_THR = 0.05,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} containing differential expression results.}

\item{GeneSymbol}{A \code{character} string giving the column name for gene symbols.}

\item{logFC_from}{A \code{character} string giving the column name for log fold-change.}

\item{Pvalue_from}{A \code{character} string giving the column name for adjusted p-values.}

\item{logFC_THR}{A \code{numeric} value specifying the log fold-change threshold.
Default is \code{1}.}

\item{P.Value_THR}{A \code{numeric} value specifying the adjusted p-value threshold.
Default is \code{0.05}.}

\item{...}{Additional arguments (currently not used).}
}
\value{
A \code{data.frame} with standardized columns:
\itemize{
\item \code{GeneSymbol} Gene symbol.
\item \code{logFC} Log fold-change.
\item \code{pval} Adjusted p-value.
\item \code{change} Differential expression status:
\code{"up"}, \code{"down"}, or \code{"stable"}.
}
}
\description{
This function takes a differential expression gene (DEG) list and reformats it
into a standardized structure for downstream analysis in the MRTools package.
Users can specify which columns contain gene symbols, log fold-change, and
adjusted p-values. The function also applies user-defined thresholds to classify
genes as \code{"up"}, \code{"down"}, or \code{"stable"}.
}
\examples{
# Example using demo dataset included in MRTools
data("DEG_demo")
DEG_list <- DEG_fit(
  data        = DEG_demo,
  GeneSymbol  = "GeneSymbol",
  logFC_from  = "logFC",
  Pvalue_from = "adj.P.Val"
)
head(DEG_list)

}
